# Planning / workflow artifacts -- kept local
.planning/

# Rust
/target
**/*.rs.bk
Cargo.lock.*

# Per-machine local cargo config (absolute target-dir override pointing
# to a different disk -- not portable across dev machines).
/.cargo/

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.local
*.key

# Logs (ichr writes to data_dir, but allow local stray logs)
*.log
