//! mineltui — terminal-UI Minecraft Java Edition launcher.
//!
//! This library crate is exported so integration tests under `tests/` can
//! link against the crate's public API. The binary entry point is in
//! `src/main.rs`.

// Module stubs — populated by later plans in phase 1.
// Each plan adds its own `pub mod <name>;` line.

pub mod persistence;
